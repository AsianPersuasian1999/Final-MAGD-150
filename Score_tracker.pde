void setup(){
  
}
void draw(){
  background(0);
  for(int i=0; i < 0; i++); {
  textSize(32);
  fill(300,200,0);
  text("Score:"+ points, 10, 30);
}

void mousePressed() {
  run simulation
  points = points +10;
  
}